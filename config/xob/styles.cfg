# Structural properties only — colors and position are generated at runtime
# by volume.sh (colors from colorctl, position from i3 focused monitor).
length      = {relative = 0.0; offset = 200;};
thickness   = 8;
outline     = 0;
border      = 2;
padding     = 2;
orientation = "horizontal";
overflow    = "proportional";
