[user]
	email = tetsu.ismt@gmail.com
	name = Tetsu-is
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[init]
	defaultBranch = main
[alias]
  merged-branch-d = !git pull origin main && git branch --merged | grep -E -v 'main' | xargs git branch -d
