# Dotfiles manifest
# List paths to track. Edit this file to add/remove entries.

[config]
# Directories under ~/.config/ to track
nvim
kitty
i3
polybar
rofi
starship
picom
cava
zsh-abbr
autorandr
xob

[home]
# Files in ~/ to track
.zshrc
.gitconfig

[mac-config]
# Directories under ~/.config/ to track on macOS
# e.g. nvim, starship, karabiner

[mac-home]
# Files in ~/ to track on macOS
# e.g. .zshrc, .gitconfig
